# PMET documentation

**[English](#en) · [汉文](#cn)**

---

<a id="en"></a>

## Contents

| | |
|---|---|
| [1. What lives here](#en-1) | [4. Refreshing the workflow audit docs](#en-4) |
| [2. Where to start](#en-2) | [5. Conventions](#en-5) |
| [3. Layout](#en-3) | |

<a id="en-1"></a>

## 1. What lives here

PMET's longer-form documentation — anything too detailed to belong in the repo-root README. Three flavours:

- **Method explanations** (`methods/`) — what PMET actually computes and why. Read these first if you're new.
- **Per-workflow audit docs** (`workflows/`) — for each of the four workflow scripts, a step-by-step walkthrough with a verification table that's regenerated by [tests/audit/](../tests/audit/). If the doc and the code disagree, the audit catches it.
- **Performance / archive** (`perf/`, `archive/`) — investigations with their results (e.g. why MinHash ships off by default) and pre-monorepo material kept for historical bisect.

[§2](#en-2) is a topic-to-document index; [§3](#en-3) is the directory tree if you'd rather browse by file.

<a id="en-2"></a>

## 2. Where to start

If you came with a question, jump straight to the right doc:

- **"What does PMET do?"** → [methods/pmet.md](methods/pmet.md)
- **"How do I run a workflow?"** → repo-root [README.md](../README.md) §4
- **"Does this workflow do what its docs claim?"** → [workflows/](workflows/) — each doc walks every stage and PASS/FAIL-stamps the verification table
- **"How are the audit docs in workflows/ kept honest?"** → [tests/audit/README.md](../tests/audit/README.md) — they are regenerated by running each workflow against canonical inputs
- **"Why is the MinHash prefilter off by default?"** → [perf/minhash_calibration.md](perf/minhash_calibration.md)

<a id="en-3"></a>

## 3. Layout

```
docs/
├── README.md                   you are here
├── deployment.md               docker-compose stack: nginx + api + worker + frontend + redis
├── methods/                    scientific method explanations
│   ├── pmet.md                    what PMET does and why (English)
│   ├── promoter-extraction.md     how promoters are derived (bilingual + head-to-head FAQ)
│   ├── naming-conventions.md      how workflow / output filenames are structured
│   └── homotypic-contract.md      on-disk schema every indexer must produce
├── workflows/                  per-workflow audit (purpose + steps + verification)
│   ├── promoter.md                scripts/workflows/promoter.sh
│   ├── intervals.md               scripts/workflows/intervals.sh
│   ├── elements.md                scripts/workflows/elements.sh
│   └── pair_only.md               scripts/workflows/pair_only.sh
├── perf/                       performance investigations
│   └── minhash_calibration.md     CIS-BP2 sweep, why MinHash prefilter ships off
├── figures/                    SVG flowcharts and example outputs
└── archive/                    pre-monorepo material kept for historical reference
    ├── pre-monorepo-pipeline-story/   the 03..07 audit docs that workflows/ replaces
    ├── core-reports/                  PMET_project benchmark / optimisation reports
    └── verification_log.md            2641-line migration journal
```

<a id="en-4"></a>

## 4. Refreshing the workflow audit docs

The four `workflows/*.md` files are not pure prose — the verification tables in them (file hashes, sizes, presence checks, PASS/FAIL verdicts) come from actually re-running each workflow. After any change to a workflow script:

```bash
# Regenerate all four docs/workflows/*.md  (~7 min total)
make test-audit

# Or refresh just one (faster — useful while iterating on a single workflow)
python3 tests/audit/generate.py promoter
```

### What gets generated

Each run rewrites `docs/workflows/<name>.md` (one of `promoter.md` / `intervals.md` / `elements.md` / `pair_only.md`). The structure inside each is the same:

- **Narrative sections** (Purpose / Biological setup / What the script does step by step / Reproducing this audit) — hand-written prose pulled from the matching template under [`tests/audit/templates/`](../tests/audit/templates/). These are what *you* maintain when the workflow changes meaning.
- **Run snapshot** — fresh on every run: the exact command that was invoked, where the output landed, file counts (`fimohits/*.bin`, `binomial_thresholds.txt` rows, etc.), the first 3 lines of `motif_output.txt`, total enriched-pair count, wall time.
- **Verification table** — the same machine, with one row per check (`OVERALL`, `motif_output.txt deterministic vs anchor`, `binomial_thresholds rows == motifs`, ...) marked PASS / WARN / FAIL.

Working files for the run land under `tests/audit/runs/<name>/` (gitignored — useful when you want to inspect what the workflow actually produced for that audit).

### What the result means

The single most useful thing on the page is the **OVERALL** line at the top of the verification section:

- **OVERALL: PASS** → numbers in the doc match what the workflow currently produces. Commit the regenerated `docs/workflows/<name>.md` along with whatever change you made.
- **OVERALL: WARN** → something non-blocking (e.g. an `elements` per-task SHA anchor was deliberately left blank waiting for first capture). The WARN row says exactly which value to paste into `tests/audit/workflows/<name>.py` to bless it.
- **OVERALL: FAIL** → a workflow output drifted from its anchor or a cross-file invariant broke. **Either** revert the workflow change (the doc was right, the code regressed), **or** update the anchor in the spec at `tests/audit/workflows/<name>.py` to record the new intentional behavior. The FAIL row tells you which file's SHA changed.

`git diff docs/workflows/` after the run is the auditable record of how the regeneration changed each doc — that's what reviewers look at in PRs.

For the data flow (how `tests/audit/lib.py` glues the spec, the template, and the `<<PLACEHOLDER>>` substitutions together) see [tests/audit/README.md](../tests/audit/README.md).

<a id="en-5"></a>

## 5. Conventions

- File names: lowercase + hyphens (`pmet.md`, `promoter-extraction.md`).
- Markdown only at the top of each subdir; figures live under `figures/`.
- Bilingual (`**[English](#en) · [汉文](#cn)**` header followed by parallel sections) is the preferred shape; legacy split-file pairs (`*.md` + `*-zh.md`) get folded into one bilingual file when next touched.

---

<a id="cn"></a>

## 目录

| | |
|---|---|
| [1. 这里放什么](#cn-1) | [4. 重跑 workflow 审计文档](#cn-4) |
| [2. 从哪开始](#cn-2) | [5. 约定](#cn-5) |
| [3. 目录结构](#cn-3) | |

<a id="cn-1"></a>

## 1. 这里放什么

PMET 长篇文档的家 —— 凡是细到不适合塞进仓库根 README 的东西都在这。三类：

- **方法解释**（`methods/`）—— PMET 到底算什么、为什么这么算。新人先看这个。
- **per-workflow 审计文档**（`workflows/`）—— 四个 workflow 脚本各一份 step-by-step 的走读，带一张由 [tests/audit/](../tests/audit/) 重生成的 verification 表。文档和代码对不上的时候，审计会逮到。
- **性能与归档**（`perf/`、`archive/`）—— 调研报告（例如为什么 MinHash 默认关闭）和 monorepo 之前留下的材料（保留给跨迁移点 bisect 用）。

[§2](#cn-2) 是按"我有什么问题"索引；[§3](#cn-3) 是按文件浏览的目录树。

<a id="cn-2"></a>

## 2. 从哪开始

带着问题来的，直接跳到对应文档：

- **"PMET 干什么？"** → [methods/pmet.md](methods/pmet.md)
- **"workflow 怎么跑？"** → 仓库根 [README.md](../README.md) §4
- **"workflow 真的像文档说的那样吗？"** → [workflows/](workflows/) —— 每份文档逐 stage 复盘，并给 verification 表打 PASS/FAIL
- **"workflows/ 的审计文档靠什么保持诚实？"** → [tests/audit/README.md](../tests/audit/README.md) —— 由每个 workflow 跑 canonical 输入重新生成
- **"MinHash 粗筛为什么默认关？"** → [perf/minhash_calibration.md](perf/minhash_calibration.md)

<a id="cn-3"></a>

## 3. 目录结构

```
docs/
├── README.md                   你正在看的就是
├── deployment.md               docker-compose 栈：nginx + api + worker + frontend + redis
├── methods/                    科学方法解释
│   ├── pmet.md                    PMET 干什么、为什么这么干（英文）
│   ├── promoter-extraction.md     如何从基因 + GFF3 派生启动子（双语 + head-to-head 答疑）
│   ├── naming-conventions.md      workflow / 输出文件名的结构
│   └── homotypic-contract.md      每个 indexer 必须产出的磁盘 schema
├── workflows/                  per-workflow 审计（用途 + 步骤 + 校验）
│   ├── promoter.md                scripts/workflows/promoter.sh
│   ├── intervals.md               scripts/workflows/intervals.sh
│   ├── elements.md                scripts/workflows/elements.sh
│   └── pair_only.md               scripts/workflows/pair_only.sh
├── perf/                       性能研究
│   └── minhash_calibration.md     CIS-BP2 sweep，为什么 MinHash 粗筛默认关闭
├── figures/                    SVG 流程图与示例输出
└── archive/                    迁 monorepo 之前的材料，留作参考
    ├── pre-monorepo-pipeline-story/   workflows/ 取代的 03..07 审计文档
    ├── core-reports/                  PMET_project benchmark / 优化报告
    └── verification_log.md            2641 行迁移日志
```

<a id="cn-4"></a>

## 4. 重跑 workflow 审计文档

`workflows/*.md` 那四份不是纯散文 —— 文档里的 verification 表（文件 hash、大小、存在性、PASS/FAIL）是真的把每个 workflow 跑一遍跑出来的。改完 workflow 脚本后：

```bash
# 重生成全部四份 docs/workflows/*.md（~7 分钟）
make test-audit

# 或只刷一份（更快，调单个 workflow 时常用）
python3 tests/audit/generate.py promoter
```

### 会生成什么

每次跑都会覆盖写一份 `docs/workflows/<name>.md`（`promoter.md` / `intervals.md` / `elements.md` / `pair_only.md` 之一）。每份的结构都一样：

- **叙事段落**（用途 / 生物学背景 / 脚本逐步做了什么 / 重跑此审计）—— 手写散文，从 [`tests/audit/templates/`](../tests/audit/templates/) 下对应的模板拉出来。这部分是 workflow 含义变了时**你**要维护的。
- **运行快照** —— 每次跑都新的：本次实际调用的命令、输出落到哪、文件计数（`fimohits/*.bin`、`binomial_thresholds.txt` 行数等）、`motif_output.txt` 前 3 行、富集对总数、wall time。
- **验证表** —— 同上，每条 check 一行（`OVERALL`、`motif_output.txt deterministic vs anchor`、`binomial_thresholds rows == motifs` 等），标 PASS / WARN / FAIL。

跑的工作文件落在 `tests/audit/runs/<name>/`（gitignored —— 想看本次审计 workflow 实际产出什么的时候有用）。

### 结果怎么读

页面里最有用的一条是验证段顶部的 **OVERALL** 行：

- **OVERALL: PASS** → 文档里的数字跟 workflow 现在产出的一致。把这次重生成的 `docs/workflows/<name>.md` 跟你的改动一起 commit。
- **OVERALL: WARN** → 非阻塞问题（比如某个 `elements` per-task SHA anchor 故意留空等首次抓取）。WARN 那一行会明确告诉你该把哪个值粘到 `tests/audit/workflows/<name>.py` 里去 bless。
- **OVERALL: FAIL** → 某个 workflow 输出偏离 anchor，或某条跨文件不变量挂了。**要么** revert workflow 改动（文档是对的，代码回归了），**要么** 改 `tests/audit/workflows/<name>.py` 里的 anchor 来登记新的有意行为。FAIL 那一行会说哪个文件的 SHA 变了。

跑完后 `git diff docs/workflows/` 是审计 - 重生成 - 文档变化的可审记录 —— PR review 时看的就是这个 diff。

数据流（`tests/audit/lib.py` 怎么把 spec、模板、`<<PLACEHOLDER>>` 替换粘起来）见 [tests/audit/README.md](../tests/audit/README.md)。

<a id="cn-5"></a>

## 5. 约定

- 文件名：小写 + 短横线（`pmet.md`、`promoter-extraction.md`）。
- 每个子目录最上面只放 markdown；图放 `figures/`。
- 首选**双语**形态（顶部 `**[English](#en) · [汉文](#cn)**`，两段平行章节）；遗留的 `*.md` + `*-zh.md` 分开两份的，下次动到就合成一份双语。
