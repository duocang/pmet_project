# FastAPI and dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic[email]>=2.5.0

# Celery and Redis
celery>=5.3.0
redis>=5.0.0

# Process tree management for thorough task cancellation. The worker spawns
# subprocess pipelines that fork their own children; killing only the top
# Popen handle leaves orphaned processes. psutil lets us walk the tree.
psutil>=5.9

# Data processing — used by both the backend (pandas) and the PMET shell
# pipeline's Python helpers (numpy pulled in transitively via pandas,
# biopython required by deduplicate.py and parse_promoter_lengths_from_fasta.py).
pandas>=2.0.0
biopython>=1.83

# Development
python-dotenv>=1.0.0

# Test-only — fastapi.TestClient delegates to starlette which needs httpx
# at import time. Listed under main reqs because the slim image is used
# by both the worker and the unit-test runner; not large enough to gate
# behind a separate dev-deps file.
httpx>=0.25
