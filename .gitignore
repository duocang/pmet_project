# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Node / Next.js
node_modules/
.next/
*.tsbuildinfo
npm-debug.log*

# C/C++ build artifacts
*.o
*.obj
*.so
*.dylib
*.a
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile.cmake

# Build outputs (binaries are produced, not tracked)
build/

# Run outputs / caches
results/
result/
logs/
*.log

# Backend runtime state
*.db
*.sqlite
*.sqlite3

# Secrets — never commit
email_credential.txt
admin_token.txt
*.pem
*.key
.env
.env.*

# Large reference genomes / annotations (keep demo only)
TAIR10.fasta
TAIR10.fasta.fai
TAIR10.gff3

# Server-side indexing cache (16G of regenerated species data)
pmet_shiny_app/data/indexing/
data/precomputed_indexes/

# Bench-scale data (not demo)
**/data/demos/promoters/indexing/bench/
**/data/demos/promoters/pairing/bench/

# Vendored heavy archives (typical in legacy/)
**/meme-*/

# Frontend build cache
.next/

# Editor
.idea/
.vscode/
*.swp
*~

# Playwright MCP runtime artefacts (per-session logs and snapshots)
.playwright-mcp/

# Branch comparison output (regeneratable)
branch-compare/
tests/audit/runs/

# Per-session scratch / matrix-run artefacts (regeneratable, often GBs)
/tmp/
/tmp_cli/

# Playwright MCP test snapshots written to repo root (transient)
/submit-*.md
/verify-*.png
/home-learn-*.png

# Local audit notes — persisted on disk for your reference, not committed
AUDIT_FINDINGS.md

tests/integration/heatmap_consistency_report.txt
